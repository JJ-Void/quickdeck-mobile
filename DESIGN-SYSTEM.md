# Визуальная система «Подряда»

Обязательный документ. Всё, что рисуется в приложении, рисуется по нему.
Числа точные: не «примерно 24», а 24. Приблизительные значения и есть то,
из-за чего два экрана одного продукта выглядят как разные продукты.

> **Как читать этот файл.** Прошлая версия говорила «тонкие линии вместо
> рамок» и «форма не должна привлекать внимание к себе». Она была написана
> под десктоп и не описывала материал — только сдержанность. Каждый раз,
> когда к ней возвращались, приложение превращалось в табличку с текстом.
> Этот документ описывает и материал, и масштаб, и внимание.

---

## 1. Пять правил

Из них выводится всё остальное. Если правка нарушает хотя бы одно — правка
неверна, даже если выглядит красиво.

**1. Один очаг на экран.** Ровно одна большая цифра или одно имя. Всё
остальное слабее — и по размеру, и по контрасту. Два «главных» элемента
гасят друг друга: глаз мечется и не выбирает ни одного. Если кажется, что
нужен второй очаг, экран надо делить надвое.

**2. Три блока на экран, три элемента в блоке.** Блок — это очаг, карточка,
группа строк. Карточка держит метку, значение и подпись. Четвёртый элемент
туда не влезает: он живёт слоем глубже.

**3. 80 % текста приглушено.** Ярким (`ink`) набирается только то, за чем
пришли. Подписи, единицы, счётчики — `mut` (52 %) и `faint` (34 %).

**4. Один акцент.** Оранжевый — единственное действие, выбранная вкладка,
«не получено». Красный — только просрочка и удаление. Больше цветов в
системе нет. Стадию называет слово, а не оттенок.

**5. Воздух — часть конструкции.** Поле карточки 24, между карточками 12,
вокруг очага 40. Плотный экран читается как таблица, а таблицу человек и
так может открыть в книге.

---

## 2. Цвет

Токены — `core/Tokens.kt`. Мимо них цвет не пишется.

| Роль | Значение | Где |
|---|---|---|
| Грунт | `#F4F5F7` | фон экрана |
| Карточка | `#FFFFFF` | единственная поверхность данных |
| Текст | `#0E1316` | 20 % текста, не больше |
| Приглушённый | `#0E1316` 52 % | подписи, значения второго плана |
| Едва заметный | `#0E1316` 34 % | метки-рубрики, счётчики |
| Разделитель | `#0E1316` 7 % | внутри карточки, где воздуха мало |
| Акцент | `#E2701A` | действие, выбор, «не получено» |
| Мягкий акцент | `#F2A93B` | только размытое пятно на фоне |
| Тревога | `#D8412F` | просрочка и удаление |
| Подложка тревоги | `#FFF6F4` | заливка тревожной карточки |

Грунт холодный и не белый намеренно: белая карточка должна от него
отделяться. Чисто белый фон делает карточки невидимыми, и экран снова
превращается в текст.

**Панель поверх чужих экранов** — единственное тёмное место в системе.
Светлая поверхность не читается ни на карте, ни на видео, ни в галерее.
Токены `panelCard`, `panelRaised`, `textOnDark` живут только там.

---

## 3. Шрифт и шкала

Manrope, веса 400–800. Один шрифт на приложение.

| Роль | Кегль | Вес | Где |
|---|---|---|---|
| `hero` | 62 | 800 | очаг экрана, один |
| `big` | 44 | 800 | значение в карточке |
| `title` | 30 | 800 | имя записи на её экране |
| `heading` | 19 | 700 | имя в списке, счётчик папки |
| `body` | 15 | 500 | основной текст |
| `small` | 13.5 | 500 | подписи под очагом |
| `label` | 11 | 700 | метка-рубрика, капслок, разрядка 1.6 |

**Шкала с разрывом.** Соседние размеры отличаются в полтора-два раза, а не
на два пункта. Шкала без разрывов — главная причина ощущения «всё в куче»:
когда всё примерно одного кегля, глазу не за что зацепиться.

Числа — всегда табличные (`tnum`). Метка-рубрика — всегда капслоком: так она
читается как ярлык раздела, а не как ещё одна строка данных.

Весь текст рисуется через `Q()`. Всё, что рисует текст мимо, однажды
разъедется по кеглю и весу.

---

## 4. Материал

**Заливка, не рамка.** Глубину даёт мягкая тень: `elevation 10`,
ambient `#0E1316` 10 %, spot `#0E1316` 15 %. Рамка остаётся только
разделителем строк внутри карточки.

**Радиусы концентрические:** внешний = внутренний + отступ.

| Элемент | Радиус |
|---|---|
| Карточка | 28 |
| Строка списка | 24 |
| Кнопка, полоса удержания | 20 |
| Иконка в квадрате | 14 |
| Плашка внутри карточки | 8 |

**Фон.** Одно мягкое размытое пятно акцента сверху. Ничего больше: ни
решётки, ни точек, ни узоров. Фон, который набирает внимание на тепловой
карте наравне с данными, — это шум.

---

## 5. Сетка

| Шаг | Значение | Где |
|---|---|---|
| `xs` | 4 | внутри строки |
| `sm` | 8 | между подписью и значением |
| `md` | 12 | между карточками (`gap`) |
| `lg` | 16 | внутри блока |
| `xl` | 24 | поле карточки и экрана |
| `xxl` | 40 | воздух вокруг очага (`heroGap`) |

Минимальная мишень 48. Между соседними мишенями 8.

---

## 6. Движение

| Событие | Время | Кривая |
|---|---|---|
| Нажатие | 140 | пружина, затухание 0.55 |
| Смена состояния | 220 | `cubic-bezier(.2,.9,.25,1)` |
| Смена экрана или слоя | 340 | та же |
| Уход | 200 | та же |
| Ступень входа | 70 | задержка между блоками |

Нажатие сжимает до 0.985 — палец чувствует отклик, не замечая анимации.
Глубже читается как «кнопка проваливается».

Переход объясняет, что изменилось, и уходит с дороги. Ничего не пульсирует
само по себе: мигающий интерфейс воспринимается как сбой.

---

## 7. Отклик в палец

`core/Feel.kt`, три удара и ни одним больше:

- `tick` (8 мс) — палец попал по цели: вкладка, строка, фильтр;
- `confirm` (18 мс) — состояние изменилось насовсем: задача, оплата, обмен;
- `warn` (двойной) — удалено или не вышло.

Вибрация подтверждает действие человека и никогда не сопровождает то, что
приложение делает само.

**Необратимое подтверждается удержанием, а не диалогом.** Полоса
заполняется под пальцем: видно, сколько осталось, и можно передумать, просто
отпустив. Диалог «вы уверены?» требует второго нажатия и потому нажимается
не глядя.

---

## 8. Навигация

Четыре вкладки внизу: Сводка, Реестр, Задачи, Ещё. Там, где их ищут все.
Смена вкладки едет в ту же сторону, куда сдвинулся выбор в нижней строке:
вкладка правее — экран приходит справа. Нырок в слой — тоже справа,
возврат — слева.

Реестр читается слоями: **папка → записи → запись**. Смысл слоёв в том,
чтобы не вникать в лишнее: сначала «что за пачка», потом «что внутри».
Плоский список это убивает.

### Колода в пузыре

Панель поверх чужих экранов — **колода карточек**, а не список:

| Слой | Что на карточке |
|---|---|
| Полка | Сводка и четыре раздела: число, имя, полоса долей |
| Пачки | Стадии договоров, отделы, заказчики объектов; «Архив» последним |
| Записи | Знак, имя, одна подпись, одна цифра, одна полоса |
| Запись | Всё о записи, прокрутка вниз, действия внизу |

Слой из одной пачки пропускается. Над карточкой, у которой внутри есть
ещё слой, видны края двух листов стопки — это читается без слов.

Жесты разведены по осям и не спорят:

- **вбок** — соседи по слою; карточки идут с поворотом, соседи гаснут;
- **тап** по центральной — вглубь, по соседней — подвести её в центр;
- **удержание** — быстрое действие: на полке и в пачках новая запись, на
  записи — изменить. Под пальцем расходится кольцо и замыкается за 0,56 с;
  отпустил раньше — ничего не случилось;
- **назад** — стрелка в шапке.

Переход между слоями — приближение: вглубь карточка вырастает из центра,
назад — отъезжает. Боковое движение занято листанием, второй смысл у него
путал бы направление.

Под лентой — точки (до 12 карточек) или полоска с «3 / 40» и одна строка
о жестах слоя.

---

## 9. Иконки

Lucide, лицензия ISC. Один набор на приложение, сетка 24×24, обводка 1.9.
Самодельные пути запрещены: недостающее берётся из пакета `lucide-static`
и добавляется в `core/Icons.kt` вместе с исходным именем.

Пути переводятся в **абсолютные координаты с явной командой у каждого
отрезка**. Склейка «поднять ведущий m в M» ломает знаки: после M неявные
пары координат тоже становятся абсолютными, и «m9 18 6-6-6-6» рисует линию
в точку (6, −6).

Знак приложения — колода: три листа стопкой, верхний оранжевый.

---

## 10. Графика данных

Полоса долей и полоса-метр не для того, чтобы по ним считать. Они
разбивают текст и дают глазу форму: много ли красного, есть ли хвост.

- **Полоса долей** — стадии договоров, отделы: 8 пт, скругление целиком,
  зазор 2 между долями, легенда не больше трёх строк;
- **Метр** — оплачено, готовность: 6 пт, дорожка белая 10 %;
- цвета стадий: лид `#5B8DB0`, договор `#B88A5A`, работа — акцент,
  приёмка `#D9B24A`, оплата `#2E9E74`, проблема — тревога. Только в
  полосах и знаках панели, не заливкой строк.

---

## 11. Слова

Кнопка называет действие глаголом: «Синхронизировать», «Задача», «Изменить
объект». Не «ОК» и не «Готово» там, где есть последствия.

**В настройках нет поясняющих строк под кнопкой.** Назначение читается из
названия, подробность вылезает по долгому нажатию. Так устроены настройки
везде, и человеку не надо заново учиться читать наш экран.

Настройки собраны в группы: короткое имя капслоком (одно-два слова) и один
пузырь со строками внутри, разделёнными волосяной линией от текста. У
строки слева знак в квадрате 36 × 36. Порядок групп — по важности:
Синхронизация, Пузырь, Данные, Сообщения.

Пустой экран говорит, что тут будет и как это завести, — одной строкой.

---

## 12. Чего в системе нет

Перечислено, потому что каждое из этого однажды уже появлялось и портило
экран:

- растровые и стилизованные цифры — контраст 1.4–2.1 при норме 4.5;
- решётка точек и любой узор на фоне;
- мозаика из четырёх равновеликих плиток — они спорят за внимание;
- адреса, стадии и виды работ в карточке сводки;
- цветные плашки-статусы на каждой строке списка;
- градиенты внутри маленьких карточек;
- второй шрифт, второй акцент, второй набор иконок.

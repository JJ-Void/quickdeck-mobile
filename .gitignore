# Сборка. Всё это создаётся заново и в репозитории только мешает.
build/
.gradle/
.cxx/
local.properties

# Подпись приложения. Попадёт в репозиторий — придётся выпускать новый ключ,
# а обновления поверх старой установки ставиться перестанут.
*.jks
*.keystore
keystore.properties
release.txt

# Android Studio и системный мусор
.idea/
*.iml
captures/
.DS_Store
Thumbs.db

# Файлы от прошлой версии с полоской у края — лежат вне src, не собираются
_removed/
*.old
local.properties
build.log

# Собранные APK и рабочая папка, куда складываются присланные файлы:
# создаётся заново, в репозитории только мешает.
*.apk
Claude outputs/
